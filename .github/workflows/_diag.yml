name: _diag-sca
on: { workflow_dispatch: {} }
permissions:
  actions: read
  contents: read
  security-events: write
  pull-requests: write
jobs:
  diag-sca:
    uses: attested-delivery/.github/.github/workflows/reusable-sca-osv.yml@aba1860be6b6ebd5c0c0a0f07160946fb0349965
    with:
      fail-on-severity: high
      scan-args: |-
        --lockfile=Cargo.lock
