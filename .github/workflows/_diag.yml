name: _diag-sca
on:
  workflow_dispatch:
permissions:
  actions: read
  contents: read
  security-events: write
  pull-requests: write
jobs:
  diag-sca:
    uses: attested-delivery/.github/.github/workflows/reusable-sca-osv.yml@94e7410b9fad3498a1e507807900edbd2269ef81
    with:
      fail-on-severity: high
      scan-args: |-
        --lockfile=Cargo.lock
